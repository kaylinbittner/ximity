<%-# We initialized @offer in the controller show method, so we can use it here -%>
<%= @offer.title %> 
<%= @offer.description %>
<%-# `image_tag` is another rails helper, like `link_to`-%>
<%-# image_tag @offer.picture %>

<%= link_to 'Edit this Offer', edit_offer_path(@offer) %> 
<%= link_to "Delete this Offer", offer_path(@offer), method: :delete %>
<%= link_to 'Back to all offers', offers_path %> 

