<div class="row justify-content-center"> 
        <%= link_to 'Create a New Request', new_request_path, class: "btn btn-primary" %>
   </div>
<div class="row">
    <div class="column">
      <div class="card">
          <% @requests.each do |r| %>
              <%-# The line below uses a Rails helper `link_to` -%>
              <%-# It takes 2 arguments, the first is what you want the link to say - the second the path the link should take -%>
              <%-# I want this link to go to the show page for o, so I pass in the instance of o so the route can extract the :id -%>
              <h5> <%= link_to r.title, request_path(r) %> </h5> 
              <p> <%= r.description %> </p>
          <% end %>
          
          <%= link_to 'Create a New Request', new_request_path %> 
      </div>
    </div>

    <div class="column">
      <div class="card">
          <% @requests.each do |r| %>
              <%-# The line below uses a Rails helper `link_to` -%>
              <%-# It takes 2 arguments, the first is what you want the link to say - the second the path the link should take -%>
              <%-# I want this link to go to the show page for o, so I pass in the instance of o so the route can extract the :id -%>
            <h5> <%= link_to r.title, request_path(r) %> </h5>
              <p> <%= r.description %> </p>
          <% end %>
          
          <%= link_to 'Create a New Request', new_request_path %> 
      </div>
    </div>

    <div class="column">
      <div class="card">
          <% @requests.each do |r| %>
              <%-# The line below uses a Rails helper `link_to` -%>
              <%-# It takes 2 arguments, the first is what you want the link to say - the second the path the link should take -%>
              <%-# I want this link to go to the show page for o, so I pass in the instance of o so the route can extract the :id -%>
             <h5> <%= link_to r.title, request_path(r) %> </h5> 
              <p> <%= r.description %> </p>
          <% end %>
          
          <%= link_to 'Create a New Request', new_request_path %> 
      </div>
    </div>

      <div class="column">
      <div class="card">
          <% @requests.each do |r| %>
              <%-# The line below uses a Rails helper `link_to` -%>
              <%-# It takes 2 arguments, the first is what you want the link to say - the second the path the link should take -%>
              <%-# I want this link to go to the show page for o, so I pass in the instance of o so the route can extract the :id -%>
             <h5> <%= link_to r.title, request_path(r) %> </h5> 
              <p> <%= r.description %> </p>
          <% end %>
          
          <%= link_to 'Create a New Request', new_request_path %> 
      </div>
    </div>

</div>  