<%-# We initialized @request in the controller show method, so we can use it here -%>
<%= @request.title %> 
<%= @request.description %>
<%-# `image_tag` is another rails helper, like `link_to`-%>
<%-# image_tag @request.picture %>

<%= link_to 'Edit this Request', edit_request_path(@request) %> 
<%= link_to "Delete this Request", request_path(@request), method: :delete %>
<%= link_to 'Back to all Requests', requests_path %> 

